{
  "name": "uspto-bulk-search",
  "version": "1.0.7",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "dev:desktop": "npm run dev --workspace=@uspto-search/desktop",
    "build:desktop": "npm run build --workspace=@uspto-search/desktop"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
